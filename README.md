# Invite Service
Service to create invite token and to manage invite

## Setup
`npm install`

## Test
`npm test`

## Run
`npm start`

## Run in docker
```
docker-compose build
docker-compose up -d
```

## API Document
`http://localhost:3000/api-docs`
